export enum RouteNames {
  Accounts = '/accounts',
  Analytics = '/analytics',
  Budgets = '/budgets',
  Categories = '/categories',
  Dashboard = '/dashboard',
  Transactions = '/transactions',
}

export const datePickerPages: string[] = [
  RouteNames.Dashboard, RouteNames.Transactions, RouteNames.Budgets, RouteNames.Analytics,
];
